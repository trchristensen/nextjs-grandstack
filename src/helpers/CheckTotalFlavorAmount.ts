export const CheckTotalFlavorAmount = (
  flavorsArray: any,
  measurementUnit: string
) => {

  switch (measurementUnit) {
    case "g":
    
        console.log(flavorsArray.reduce());

      break;
  
    default:
      break;
  }

  return false;
};